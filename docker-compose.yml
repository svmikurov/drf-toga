services:

  drf-project:
    container_name: drf-project
    image: drf-project
    volumes:
      - ./drf_project:/drf_project
    build:
      context: .
      dockerfile: ./docker/drf-project/Dockerfile
    ports:
      - "8000:8000"

  drf-toga-redis:
    container_name: drf-toga-redis
    image: drf-toga-redis
    build:
      context: .
      dockerfile: ./docker/redis/Dockerfile
    hostname: redis
    ports:
      - "6379:6379"
